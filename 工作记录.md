# 工作记录

> 每次重要改动或决策记录。重复内容不记，不重要内容不记。

---

## 2026-07-05

### 项目启动
- 在 `D:\Claude Code\Product Manager\web-Breview\` 建立项目工作区
- 安装 3 套技能到 Product Manager 级别，所有子项目共享：
  - **impeccable**（v3.9.1）：UI 设计全套，23 个命令
  - **karpathy-guidelines**：编码行为准则
  - **ui-ux-pro-max 系列**（7 个技能）：banner-design / brand / design / design-system / slides / ui-styling / ui-ux-pro-max

### 读取的资料
- web-dev-brief.md：完整的项目需求文档
- 用户档案：大二学生，前后端小白，暑期全职创业

### 完整前端搭建
- 技术栈：React 19 + Vite 8.1.3 + Tailwind CSS v4
- 配色方案：淡橘黄 + 白色为主，淡蓝点缀，橙/蓝/粉三色卡片
- 页面布局：标题 → 表单区 → 结果区（分析摘要 + 三卡片叠层）→ 底部
- 组件清单：App / InputForm / FieldExample / ResultArea / ReplyCardStack / ReplyCard / AnalysisSummary
- 工具模块：api.js（Coze 调用）、storage.js（每日60次限制）
- 自定义动画：翻卡片示例、卡片叠层切换、结果区滑入、复制反馈
- 命名品牌改为 ReplyMate（用户后续要求）

### 安全加固
- 发现 Token 直接写在前端代码的安全隐患
- 将 Token 迁移至 .env 文件
- 搭建 Vite 开发代理：浏览器 → Vite 代理 → Coze API，Token 不暴露给客户端
- 修复 API URL：stream_run → run（非流式接口）
- .env 加入 .gitignore

---

## 2026-07-06

### 品牌与UI迭代
- 品牌名从 Breview Reply 改为 **ReplyMate**
- 配色更新：背景白色，强调色 #1660AB（深蓝），卡片三色玻璃（橙/蓝/粉）
- 背景装饰：橘/红/深蓝/黑五色模糊圆，透明度 0.45
- 左侧边栏导航（差评输入 / 回复生成）+ 可折叠历史记录
- 回复生成区去掉白色方框，直接展示三张卡片
- "换一个版本"按钮移到卡片右侧，垂直居中

### 历史记录功能
- 新增 `src/utils/history.js`，localStorage 存储，最多 20 条
- 侧边栏可展开查看、点击加载、删除
- 空状态页展示最近 3 条历史快捷入口

### 安全加固
- Token 从源码抽离到 `.env` + Vite 代理，浏览器不可见
- Coze API 返回格式适配（`data` 字段直接为 JSON 字符串）

### 部署准备（第六步）
- 新建 `api/coze.js`：Vercel Serverless 函数代理 Coze API
- 新建 `vercel.json`：Vercel 路由配置 + SPA 回退

---

## 格式约定

```
## YYYY-MM-DD

### 做了什么
- 重要改动、新建文件、关键决策

### 决定了什么
- 已做出的选择、方向调整

### 下一步
- 待办事项
```
